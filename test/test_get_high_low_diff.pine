// Get high / low / diff for each candle.
//@version=5

indicator('Previous Candle High and Low', shorttitle='Prev. H/L', overlay=true)
diff = high-low
plot(series = diff, style=plot.style_columns)
// plot(high)
// plot(low)